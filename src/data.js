export const SIGNS = Array.from({ length: 52 }, (_, i) => ({
  id: i + 1,
  title: `Признак ${i + 1}`,
  description: `Краткое описание признака ${i + 1}. Здесь можно вставить полный текст и детали.`
}));
